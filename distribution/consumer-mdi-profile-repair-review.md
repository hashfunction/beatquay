# Installed MDI workflow repair — 2026-09-12

Candidate starts at local `b2fd093a13641eab219efa300b12142883cb7217`. This is an observer repair, with no app C++, branding, dependency, package identity, publication, or source-license changes. Fresh installed Windows acceptance is still required.

Run `34678072633` built public source `ffcedc4d3b9e9e5fa36a5bc7c1bebd428b08e427` with Qt 6.11.2. Its 22 native CTests and repeated lifecycle test passed; independent native render/error and original starter renders passed. MSIX signing, Add-Appx, exact retained process/package identity, working-directory/Settings first run, and 106 package/system module checks passed. The normal File → New from template → BeatQuay-Drum-Grid action opened all three authored tracks in PID 7720. It then failed before tempo entry/save/export: `Expected one owned Button input: / / Maximize; found 0`. Acceptance remains false; cleanup terminated that retained process with exit -1, removed package registration, and restored display/working directory. It did not prove normal zero close or the full uninstall acceptance path, and preserved the changed profile.

Evidence reviewed is under `/private/tmp/beatquay-34678072633-review/build-evidence/msix-install`: `accessible-window-tree.json`, `consumer-workflow/consumer-workflow.json`, and `installation-qualification.json`. The compact committed fixture copies five exact control snapshots from the `original_template_opened` stage at `2026-09-12T06:46:00.1649884Z`; it does not invent an accessible title button or tooltip. The observed Song-Editor is an enabled Window of class `lmms::gui::SubWindow` at `(261,166,333,163)`. Its direct content is `lmms::gui::SongEditorWindow` at `(265,190,325,135)`; its QMdiArea is `(261,166,1427,816)`.

`src/gui/SubWindow.cpp` creates custom 17px title buttons directly under the MDI subwindow, with no-focus policy and a mouse-transparent title label. The exact cached [Qt 6.11.2 source archive](https://download.qt.io/official_releases/qt/6.11/6.11.2/submodules/qtbase-everywhere-src-6.11.2.tar.xz), `src/widgets/accessible/qaccessiblewidgets.cpp:440–453`, establishes the missing provider route: `QAccessibleMdiSubWindow` exposes only its content widget. Its title buttons are not accessible children. `src/widgets/widgets/qmdisubwindow.cpp:3191–3232` supplies the inherited normal title-bar double-click route. `setNewWindowTitle` at line 263 also merges the exact ` - [Song-Editor]` suffix into the main title. The observer now uses that native double-click route, computes its point from live outer/content rectangles, checks exact class/type/ID/PID/parent bounds, and requires UIA's point hit to compare equal to the retained Song-Editor before **each** click. [Microsoft documents the AutomationElement comparison overload](https://learn.microsoft.com/en-us/dotnet/api/system.windows.automation.automation.compare?view=windowsdesktop-10.0). Native foreground/PID and window-at-point checks remain at input. Geometry changes between clicks refuse further input. A 30-second observation must prove the exact maximized title and full MDI-area geometry before enabling that precise suffix for subsequent expected main titles and capture records. No WindowPattern mutation of the owning top-level QWindow is used.

The same actual tree exposes the tempo as an enabled Group of class `lmms::gui::LcdSpinBox`, with the unique full ID `QApplication.lmms::gui::MainWindow.QWidget.mainToolbar.lmms::gui::LcdSpinBox`, and empty HelpText. `src/gui/editors/SongEditor.cpp:137–140` binds precisely this widget to the tempo model. The observer selects that exact native identity instead of the unavailable HelpText. The normal Set value dialog must still expose original 112 before entering 116. Click revalidation now also refuses a changed class. All original project semantics, 55 notes/three tracks, saved/reopened bytes, actual export UI, duration/audio checks, Kicker/runtime binding, normal-close and cleanup gates remain mandatory.

The profile had only its first-run hash at `06:45:46.6056001Z`. Source `Song.cpp:1181` calls `ConfigManager::addRecentlyOpenedProject`; `ConfigManager.cpp:322–333` immediately saves the config when loading the template. The next old attribution checkpoint was after Save, which this run never reached. This is a proven missing checkpoint; the final XML itself was not retained, so this review cannot retrospectively claim its only delta was a recent-file entry. The new observer verifies it immediately after the UI proves the template is open, and makes a read-only verification attempt while the exact retained process is still live if the workflow fails. It uses the unchanged XML allowlist, initial baseline, activation interval, creation-time and before/after hash checks. Failure is separately recorded and preserves the primary UI exception and last attributable hash. Deletion still requires observed original process termination and the unchanged last verified hash; it now rechecks creation time too. Foreign/replaced settings remain preserved.

Local verification:

- `TMPDIR=/private/tmp <pwsh-7.6.6> -NoLogo -NoProfile -File cmake/msix/test_consumer_workflow.ps1`: production control/export/profile boundaries; exact captured title geometry with 14 refusal variants; direct-child provider selection with six refusal variants; exact native tempo selector with six refusal variants; production two-click ordering, per-click foreign-hit/movement refusals, no success without filled-area geometry, exact post-maximization title handling, and six failure-profile refusal variants. Python verifier rejection tracebacks are intentional negative cases.
- All 11 PowerShell fixtures from `cmake/msix/qualify-msix.ps1` passed in separate processes, as the real runner invokes them: orchestration, recorded Appx import preflight, evidence, serialized registration ownership, process observation, window evidence, first run, Defender provenance, temporary ownership, consumer workflow, display restoration. macOS `/var` is a symlink; setting `TMPDIR=/private/tmp` keeps the existing reparse guard intact. Running these fixtures in one shared PowerShell process is invalid because several deliberately define isolation-local mocks.
- With the existing PowerShell directory on `PATH`, `python3 -m unittest discover -s cmake/msix -p 'test_*.py' -v`: **31 passed**, covering independent project/WAV/profile checks, source/package binding and actual PE-collector ordering. Initial collector invocation without `pwsh` on PATH failed solely to locate that executable; the corrected invocation passed.
- `QT_QPA_PLATFORM=offscreen <Twin env-qt6/bin/python> cmake/msix/manual-probes/qt_mdi_title.py`: real installed Qt **runtime 6.11.2**, Fusion/QMainWindow/QMdiSubWindow, native Qt event route maximized to `(0,0,1000,675)` with content filling the same area, and exact suffix persisted through untitled/saved/new title updates. This is a bounded Qt behavior probe, not the Beat app, Windows UIA or an installed acceptance substitute.
- PowerShell syntax parsing and `git diff --check` passed. No large downloads or native rebuild were required locally.

Exact reusable runtimes: PowerShell is `/Users/hashfunction/workspace/project_app_factory/microsoft-store/apps/filequay/source/.tools/powershell-7.6.6/pwsh`; the Qt probe used `/Users/hashfunction/workspace/project_app_factory/microsoft-store/apps/twinquay/source/env-qt6/bin/python` (PyQt6 6.11.0, Qt runtime 6.11.2). Native Windows must still verify the real UIA hit and double-click timing, updated title flow, original 112→116 edit, all subsequent real dialogs/save/reopen/export, actual profile delta, normal zero close and owned cleanup. No screenshots, consumer workflow success, Store readiness or public release are claimed from local replays.
