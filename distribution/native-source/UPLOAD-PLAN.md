# Prepared native source upload plan

Repository: `hashfunction/beatquay`. Proposed release tag: `beatsprig-native-source-1.0.1`. No release or upload is created by this plan or by the source validator.

1. Independently review the exact 17 `source-release.json` rows, all original notices and the final public application commit. Run `python distribution/native_source.py verify-archives --archives /private/tmp/beatsprig-corresponding-source/archives`. Keep the JSON receipt with the release preparation; every original archive must match SHA256, SHA512 and bytes.
2. Prepare a GitHub draft release at the reviewed public source commit, with the 17 original archive filenames unchanged. Include `source-release.json`, this build/source guide, the original notice bundle and its hash inventory. The total original source payload is 108,354,372 bytes. The application source itself is the exact public Git commit and its generated source archive; hiir/ringbuffer are supplied explicitly rather than relying on empty Git submodule directories.
3. Read back every uploaded asset and compare its actual bytes to the manifest before changing draft state. Publish only after review. Then anonymously download all 17 public URLs into an owned temporary directory, run the same verifier, and record the GitHub release/asset IDs, public URLs, source commit, hashes and verification timestamp. Do not mark source delivery complete from successful upload requests alone.
4. Bind that independently verified public receipt and the current source/runtime inventory to the final package. Keep this manifest's historical `prepared-unpublished` state intact as its preparation record; a separate verified delivery receipt records what was actually published. Reissue the manifest and release if any original input changes. A title/version match is insufficient.

The latest candidate's consumer XML repair does not justify rewriting historical native results. Use the exact runtime/source/input equality records to carry forward unchanged source material, and require current package qualification separately. No binary, certificate, profile payload, source status flag or Store submission is authorized by this upload-plan file itself.
