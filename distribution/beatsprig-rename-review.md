# BeatSprig 1.0.1 rename review — 2026-09-12

This customer rename follows the separate tempo-observer repair `1706958724b997c9669ca17bde0590dafa09bf7e`. It changes product identity and corresponding build/package/UI expectations. It does not claim a new native or installed Windows success.

The product is **BeatSprig 1.0.1**, publisher **Trieflow LLC**, canonical `https://beatsprig.trieflow.com` with `/privacy` and `/support`. Shared identity constants update the actual main/project/settings/recovery/About/splash and version text. CLI help now displays its actual invoked executable name. Windows numeric resource and MSIX version are `1.0.1.0`. The public Windows host becomes `beatsprig.exe`; NSIS links/file associations and installed external/visual manifests follow that basename, CPack retains its existing release gates and uses the new product name/version, and the temporary MSIX/artifact names use BeatSprig.

Windows instruments import symbols from the executable. This therefore requires a complete rebuild of the existing three instruments, not a post-build rename of old binaries. The production `lmms` CMake target and the real `BeatQuayTemplateTest` host both call the same Windows output-name helper. `BuildPlugin.cmake` continues to link the real host target. The native test still requires exactly the current process's host module and the real Kicker/TripleOscillator modules, now under `beatsprig.exe`. Package PE validation requires that import for **all three** approved instruments and rejects a stale `lmms.exe` import. The normal render command, installed process path/hash checks and package manifest all use the renamed host. C++ namespaces, core symbols and upstream XML/schema versions remain unchanged. Non-Windows executable basenames are outside this Windows rename and retain their existing behavior.

Compatibility is explicit:

- Assigned Store package identity remains `1659hashfunction.BeatQuay`; ApplicationId remains `BeatQuay`. This source's actual installed qualifier continues to use its separate CI identity `Trieflow.BeatQuay.Qualification` / `CN=BeatQuay-CI-Qualification`, with its existing ApplicationId and new version. No Store identity is inferred from that disposable package and no new Store/export acceptance is claimed.
- ConfigManager's `.beatquayrc.xml`, Documents `BeatQuay`, and portable `beatquay-workspace` paths are unchanged. The first-run question says **BeatSprig** while requesting the same compatible folder. Ownership/creation-time/marker/content/cleanup requirements and all exact window expectations remain mandatory.
- The original generated starters are now `BeatSprig-Drum-Grid.mpt` and `BeatSprig-Bassline-Sketch.mpt`, which supplies the real normal menu labels. Apart from the generator creator attribute and provenance comment, parsed XML is identical to the pre-rename files: same 55/40 notes, voices, tempo, timing, schema and synthesis parameters. Existing user projects/config paths are not rewritten. The CC0 dedication is unchanged. The new provenance contains both current hashes and the verbatim original authorship record; `starter-inputs.json` retains the prior generator/file hashes under their exact pre-rename source commit, separately from the current input map.
- Qt/vcpkg/submodule pins, Qt notices, dependency/source history, prior native run claims, captured native UI fixtures and upstream README remain byte-for-byte. Historical reports are preserved; the current product-identity page explicitly labels its older sections historical. MDI replay tests apply the new title to a copy of the old captured snapshot; they do not rewrite old Windows evidence.
- The existing original SVG/PNG/ICO have no written product name and remain byte-for-byte, with their internal `beatquay` resource paths retained. Current branding documentation identifies BeatSprig. Genuine new Windows screenshots are still pending, and the marketing-review flag stays true.

Local verification:

- Updated identity expectations failed before the rename, then passed. `python3 -m unittest discover -s tests/scripted -p test_product_identity.py -v`: **4 passed**, exercising actual CMake resource configuration, customer/test host output names, compatible settings/workspace constants, CPack display/version/install folder, actual installed artwork and the renamed Windows manifests/shortcuts/file-association targets.
- Reconfigured/rebuilt existing `.cache/product-identity` against actual Qt **6.11.2**, then `ctest --test-dir .cache/product-identity --output-on-failure`: **3 suites passed**. These compile the real ConfigManager/PathUtil/ProjectVersion with the already documented headless dependency boundary, the actual extracted production title method, and actual embedded icon/application identity. Their platform-home/config/sentinel checks remain unchanged; they do not launch the full Windows app.
- `python3 -m unittest discover -s tests/scripted -p 'test_starter*.py' -v`: **22 passed**. Actual temporary CMake installation, exact deterministic source bytes/input hashes, no-overwrite generation, altered/extra source/stage rejection and existing audio/CLI failure checks remain active. `test_candidate_render.py`: **13 passed**.
- With the existing PowerShell directory on PATH, `python3 -m unittest discover -s cmake/msix -p 'test_*.py' -v`: **32 passed**, including independent 116-BPM project/reopen/audio checks, exact new manifest, actual PowerShell PE collection ordering, and explicit rejection of a plugin importing the old host.
- All **12** PowerShell suites in `qualify-msix.ps1` passed in separate processes with `TMPDIR=/private/tmp`: orchestration, Appx preflight, evidence, registration ownership, process observation, window evidence, first run, Defender provenance, temporary ownership, consumer workflow, tempo and display restoration. First-run tests include an independently written literal BeatSprig question with the retained `Documents/BeatQuay/` path.
- `src/core/main.cpp` passed the prior recorded real Qt 6.11.2 Apple syntax command with `-std=c++20 -Wall -Werror -fsyntax-only` and the existing bounded dependency headers. Both original artwork and renamed starter generators passed `--check`. Historical bytes were compared against the pre-rename commit. `git diff --check` passed.

Reusable runtimes: PowerShell `/Users/hashfunction/workspace/project_app_factory/microsoft-store/apps/filequay/source/.tools/powershell-7.6.6/pwsh`; Qt CMake package `/opt/homebrew/lib/cmake/Qt6`; the small Qt test build uses `.cache/task4-syntax/samplerate.h`. No large download or full local app build was needed.

Fresh exact-source Windows must compile/link the new host and all instruments, pass the full native suite, stage/verify/sign/install the disposable package, complete the normal first run, original starter selection, exact 112→116 tempo route, save/reopen and musical WAV export, capture the real new-brand UI, close normally and uninstall/clean up. Source/license closure, physical speaker output, WACK and Store submission remain separate unproven gates. No public push, parent status, Store/account mutation, website deployment or new public native-source asset was performed here.
