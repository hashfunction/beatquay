# BeatSprig native source integration plan

**Goal:** Bind original runtime notices, prepared corresponding-source delivery, and exact Microsoft runtime origins into the existing candidate package without changing consumer behavior.

**Architecture:** Extend the existing hashed Qt notice bundle and copy a separate original app-dependency notice bundle. A source-delivery manifest lists exactly the 17 reviewed archives, independently validated against actual Qt/vcpkg/module inputs. CMake records the release runtime source paths it selected; a Windows observer hashes those exact files and their staged copies and records version/terms metadata, with independent Python validation before packaging.

**Scope:** Existing source assessment is `../../../../native-source-collection/README.md`. Public release preparation remains unpublished. Original GPLv2 application files stay intact; the combined executable carries GPLv3 terms because ringbuffer is compiled into core. All consumer, native, ownership, cleanup, and package gates remain intact. The running Windows commit is unchanged.

- [x] Extend `distribution/collect_qt_notices.py` to accept only explicitly inventoried original attribution/license paths, verify Git blob hashes, validate all JSON file references without executing/normalizing originals, and reject symlink parents or escaping paths. Add focused tests to `tests/scripted/test_qt_notices.py` for actual raw-control JSON, missing referenced notices, tampered Git blob, extra file, and parent symlink. Preserve all existing tests.
- [x] Materialize exact reviewed original Qt and compiled app-dependency notices; add `distribution/native-source/COMBINED-LICENSE.md`, GPLv3 text, current-source build/install instructions, exact 17-archive release manifest and upload plan. Add an offline validator with tests for changed archive bytes/size/SHA512, missing/duplicate owners, changed module pins, missing archive and staged runtime mapping. Keep publication/closure false until separate verified delivery.
- [x] In `BeatQuayRuntime.cmake`, serialize only the release `CMAKE_INSTALL_SYSTEM_RUNTIME_LIBS` paths and selector roots after the existing discovery call. Keep both existing release/debug install blocks unchanged. Add `collect-ms-runtime-origins.ps1` and a Python validator that require the exact staged Microsoft filename set, source path containment in release CRT or versioned x64 UCRT redist directories, matching source/stage bytes and hashes, source file versions, and original selector-file hash. Add real filesystem/CMake and metadata-mutation tests.
- [x] Invoke notice/source/origin collection from `distribution/qualify-candidate.ps1` after native success, bind every new input/evidence file through `msix_qualification.py`, and retain the origins JSON as metadata. Package both original and combined-work terms. Exercise package fixture tests with explicit missing/tampered evidence failures, rerun existing focused notice/runtime/package tests, record Windows pending, and commit only this candidate source.

Execution is inline in the assigned repository; root independently reviews and owns publication.

The separately reviewed export-combo fixture is invoked beside the existing consumer workflow fixture. About now presents combined GPLv3 and complete GPLv3 text before the preserved original application license. A real Qt resource harness executes the production resource list, text reader and About statement. See `native-source-integration-report.md` for results and Windows/publication limits.
