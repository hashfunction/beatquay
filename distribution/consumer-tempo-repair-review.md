# Consumer tempo route candidate — 2026-09-12

Starting source: `72f95d0e58e9508a94c9c304471167570294cbf1`. This change affects the installed qualification observer only. It does not change the app, its tempo model, or package/runtime inputs.

Windows run `34681627248`, public source `d79b36432a691c2a30ae5abfde99d5c45506b42f`, successfully opened the original Drum Grid and maximized the actual Song-Editor. It failed at `edit_tempo_116`: `Exact complete owned consumer window did not appear: Set value`. The exact live Group `lmms::gui::LcdSpinBox` under `QApplication.lmms::gui::MainWindow.QWidget.mainToolbar` was visible/enabled in PID 3772 at `(480,102,45,30)`; the recorded two-click point was `(502,117)`. The last complete observation contains only the main window. No tempo edit, save, export or normal consumer completion is established by this failure.

The compact fixture `cmake/msix/fixtures/tempo-34681627248.json` copies the exact main/tempo snapshots and binds the original JSON hash. Source `LcdSpinBox.cpp:155–179` unconditionally calls the normal `QInputDialog::getInt` from its double-click handler; there is no excluded label area in that handler. [Qt 6.11.2's input dispatcher](https://github.com/qt/qtbase/blob/v6.11.2/src/gui/kernel/qguiapplication.cpp) classifies double-clicks using press timestamps, distance/button state and the last press window. The receipt has no individual native press timestamps or actual event delivery trace, so it cannot distinguish those causes. This candidate does not claim a proven product double-click defect or guess a timing delay.

The approved alternative uses normal customer input already implemented by the same widget. `LcdSpinBox.cpp:144–152` increments one model step for each positive wheel event; Song's tempo is an integer model with step 1. `LcdSpinBox::contextMenuEvent`, `CaptionMenu.cpp`, and `AutomatableModelView::addDefaultActions` expose a captioned **Tempo** menu with a disabled **Tempo** caption and **Copy value (N)** action. The observer reads that action without invoking it or writing the clipboard. It requires 112 first, then sends exactly four positive 120-unit native wheel events, requiring 113, 114, 115 and 116 from a fresh menu after each. This prevents event coalescing or an ignored detent from silently satisfying the workflow. A wrong starting value, missing menu, wrong intermediate value, ambiguous provider, or failed dismissal stops the workflow. There is no retry-until-target loop.

Each context click and wheel action rechecks the retained process/package, exact live class/automation ancestry, finite in-main geometry, exact UIA hit equality, native foreground HWND, PID at the point, and native root HWND after cursor placement. Successful inputs receive separate timestamped metadata. Each menu must be the exact complete owned `lmms::gui::CaptionMenu` with the source-defined caption and value action; Escape requires the exact main/menu foreground and observed menu PID, then disappearance must be observed. The existing title/MDI proof, independent saved and reopened project tempo 116, all notes/tracks, WAV musical content/duration, exact installed files/modules, profile attribution, normal zero close, uninstall and cleanup gates remain in force.

Validation:

- Added `test_consumer_tempo.ps1` failed first because the production route did not yet exist, then passed: captured native geometry plus 12 refusals; five native ownership refusals; seven menu refusals; production pointer ordering, receipt and four fail-before-success boundaries; exact four-detent sequence and six failure stops. The context-menu fixture is explicitly a source-defined hypothetical provider record, not captured Windows evidence.
- Existing `test_consumer_workflow.ps1` passed, including actual MDI/provider/profile/cleanup boundaries. Expected negative profile verifier errors remain intentional.
- `python3 -m unittest discover -s cmake/msix -p 'test_consumer_files.py' -v`: all 11 passed, including unchanged-tempo, different reopened project, altered notes/instrument, silence and partial-song rejection.
- Both PowerShell suites compile the real native input helper. `git diff --check` passed. The new suite is included in the real `qualify-msix.ps1` fixture list.

PowerShell runtime used: `/Users/hashfunction/workspace/project_app_factory/microsoft-store/apps/filequay/source/.tools/powershell-7.6.6/pwsh`, with `TMPDIR=/private/tmp`. Fresh installed Windows must still establish the actual context-menu UIA shape and normal wheel delivery, complete save/reopen/export, and normal close/uninstall. No synthetic UI receipt or new Windows success is claimed. Customer rename is deliberately a separate commit.
