Original published BeatSprig native-source delivery and preparation JSON, retained
byte-for-byte for offline validation tests. The preparation flags are historical
and intentionally remain false. Tests use controlled network measurements;
production verification anonymously downloads and rehashes every public asset.
The public receipt's exact hash is pinned in distribution/native-source-publication.json.
